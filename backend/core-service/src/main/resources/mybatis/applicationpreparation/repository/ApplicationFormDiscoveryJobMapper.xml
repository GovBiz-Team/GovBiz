<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.govbiz.core.applicationpreparation.repository.mapper.ApplicationFormDiscoveryJobMapper">
    <resultMap id="job" type="ai.govbiz.core.applicationpreparation.repository.mapper.ApplicationFormDiscoveryJobDbRow">
        <id column="id" property="id" />
        <result column="owner_account_id" property="ownerAccountId" />
        <result column="request_key" property="requestKey" />
        <result column="source_code" property="sourceCode" />
        <result column="source_program_id" property="sourceProgramId" />
        <result column="program_title" property="programTitle" />
        <result column="program_source_url" property="programSourceUrl" />
        <result column="status" property="status" />
        <result column="result_json" property="resultJson" />
        <result column="failure_code" property="failureCode" />
        <result column="created_at" property="createdAt" />
    </resultMap>
    <sql id="summary">
        id, owner_account_id, request_key, source_code, source_program_id,
        COALESCE(
            (SELECT p.title FROM support_program p
             WHERE p.source_code = application_form_discovery_job.source_code
               AND p.source_program_id = application_form_discovery_job.source_program_id),
            '공고 정보를 확인하지 못한 작업'
        ) AS program_title,
        (SELECT p.source_url FROM support_program p
         WHERE p.source_code = application_form_discovery_job.source_code
           AND p.source_program_id = application_form_discovery_job.source_program_id) AS program_source_url,
        status, failure_code, created_at
    </sql>
    <select id="lockActiveAccount" resultType="long">
        SELECT id FROM account WHERE id = #{ownerId} AND deleted_at IS NULL AND suspended_at IS NULL FOR UPDATE
    </select>
    <select id="findRequest" resultMap="job">
        SELECT <include refid="summary" />, result_json FROM application_form_discovery_job
        WHERE owner_account_id = #{ownerId} AND request_key = #{requestKey}
    </select>
    <select id="findActive" resultMap="job">
        SELECT <include refid="summary" />, result_json FROM application_form_discovery_job
        WHERE source_code = #{sourceCode} AND source_program_id = #{sourceProgramId} AND active_slot = 1
    </select>
    <select id="countPending" resultType="int">
        SELECT COUNT(*) FROM application_form_discovery_job WHERE owner_account_id = #{ownerId} AND active_slot = 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO application_form_discovery_job (owner_account_id, request_key, source_code, source_program_id, created_at, next_publish_at)
        VALUES (#{ownerAccountId}, #{requestKey}, #{sourceCode}, #{sourceProgramId}, #{createdAt}, #{createdAt})
    </insert>
    <select id="find" resultMap="job">
        SELECT <include refid="summary" />, result_json FROM application_form_discovery_job WHERE id = #{id}
    </select>
    <select id="findOwned" resultMap="job">
        SELECT <include refid="summary" />, result_json FROM application_form_discovery_job WHERE id = #{id} AND owner_account_id = #{ownerId}
    </select>
    <select id="listOwned" resultMap="job">
        SELECT <include refid="summary" /> FROM application_form_discovery_job WHERE owner_account_id = #{ownerId} ORDER BY id DESC LIMIT 20
    </select>
    <update id="claim">
        UPDATE application_form_discovery_job j INNER JOIN account a ON a.id = j.owner_account_id
        SET j.status = 'RUNNING', j.started_at = #{now}
        WHERE j.id = #{id} AND j.status = 'QUEUED' AND j.created_at &gt; DATE_SUB(#{now}, INTERVAL 1 HOUR)
          AND a.deleted_at IS NULL AND a.suspended_at IS NULL
    </update>
    <update id="beginAi">
        UPDATE application_form_discovery_job j INNER JOIN account a ON a.id = j.owner_account_id
        SET j.ai_started_at = #{now}
        WHERE j.id = #{id} AND j.status = 'RUNNING' AND j.ai_started_at IS NULL
          AND j.started_at &gt; DATE_SUB(#{now}, INTERVAL 20 MINUTE)
          AND a.deleted_at IS NULL AND a.suspended_at IS NULL
    </update>
    <update id="finish">
        UPDATE application_form_discovery_job SET status = #{status}, result_json = #{resultJson}, failure_code = #{failureCode}, finished_at = #{now}
        WHERE id = #{id} AND status = 'RUNNING' AND started_at &gt; DATE_SUB(#{now}, INTERVAL 20 MINUTE)
    </update>
    <select id="publishable" resultType="long">
        SELECT id FROM application_form_discovery_job WHERE status = 'QUEUED'
        AND created_at &gt; DATE_SUB(#{now}, INTERVAL 1 HOUR) AND next_publish_at &lt;= #{now} ORDER BY id LIMIT 20
    </select>
    <update id="reservePublication">
        UPDATE application_form_discovery_job SET next_publish_at = DATE_ADD(#{now}, INTERVAL 1 MINUTE)
        WHERE id = #{id} AND status = 'QUEUED' AND created_at &gt; DATE_SUB(#{now}, INTERVAL 1 HOUR) AND next_publish_at &lt;= #{now}
    </update>
    <update id="markPublished">
        UPDATE application_form_discovery_job SET last_published_at = #{now} WHERE id = #{id}
    </update>
    <update id="expireQueued">
        UPDATE application_form_discovery_job j INNER JOIN account a ON a.id = j.owner_account_id
        SET j.status = 'FAILED', j.failure_code = CASE WHEN a.deleted_at IS NOT NULL OR a.suspended_at IS NOT NULL THEN 'ACCOUNT_INACTIVE' ELSE 'QUEUE_EXPIRED' END,
            j.finished_at = #{now}
        WHERE j.status = 'QUEUED' AND (j.created_at &lt;= DATE_SUB(#{now}, INTERVAL 1 HOUR) OR a.deleted_at IS NOT NULL OR a.suspended_at IS NOT NULL)
    </update>
    <update id="expireRunning">
        UPDATE application_form_discovery_job SET status = 'UNKNOWN', failure_code = 'RUN_OUTCOME_UNKNOWN', finished_at = #{now}
        WHERE status = 'RUNNING' AND started_at &lt;= DATE_SUB(#{now}, INTERVAL 20 MINUTE)
    </update>
</mapper>
