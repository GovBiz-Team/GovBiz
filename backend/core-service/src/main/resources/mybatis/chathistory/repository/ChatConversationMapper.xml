<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.govbiz.core.chathistory.repository.mapper.ChatConversationMapper">
    <resultMap id="row" type="ai.govbiz.core.chathistory.repository.mapper.ChatConversationDbRow">
        <id column="id" property="id" />
        <result column="account_id" property="accountId" />
        <result column="conversation_id" property="conversationId" />
        <result column="title" property="title" />
        <result column="snapshot" property="snapshotJson" />
        <result column="version" property="version" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>
    <select id="lockActiveAccount" resultType="long">
        SELECT id FROM account WHERE id = #{accountId} AND deleted_at IS NULL AND suspended_at IS NULL FOR UPDATE
    </select>
    <select id="findPage" resultMap="row">
        SELECT id, account_id, conversation_id, title, version, updated_at
        FROM chat_conversation
        WHERE account_id = #{accountId} AND deleted_at IS NULL
        <if test="before != null">AND id &lt; #{before}</if>
        ORDER BY id DESC LIMIT 31
    </select>
    <select id="find" resultMap="row">
        SELECT id, account_id, conversation_id, title, snapshot, version, updated_at
        FROM chat_conversation WHERE account_id = #{accountId} AND conversation_id = #{conversationId} AND deleted_at IS NULL
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_conversation (account_id, conversation_id, title, snapshot, version, updated_at)
        VALUES (#{accountId}, #{conversationId}, #{title}, #{snapshotJson}, #{version}, #{updatedAt})
    </insert>
    <update id="update">
        UPDATE chat_conversation SET title = #{title}, snapshot = #{snapshotJson}, version = #{version}, updated_at = #{updatedAt}
        WHERE account_id = #{accountId} AND conversation_id = #{conversationId}
    </update>
    <delete id="deleteByAccountId">DELETE FROM chat_conversation WHERE account_id = #{accountId}</delete>
    <select id="isDeleted" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM chat_conversation
            WHERE account_id = #{accountId} AND conversation_id = #{conversationId} AND deleted_at IS NOT NULL)
    </select>
    <!-- 내용은 제거하고 식별자만 보존해 다른 탭·재시도의 늦은 저장을 차단합니다. 반복 삭제도 같은 결과입니다. -->
    <insert id="markDeleted">
        INSERT INTO chat_conversation (account_id, conversation_id, title, snapshot, version, updated_at, deleted_at)
        VALUES (#{accountId}, #{conversationId}, '', JSON_OBJECT(), 1, #{deletedAt}, #{deletedAt})
        ON DUPLICATE KEY UPDATE title = '', snapshot = JSON_OBJECT(),
            updated_at = COALESCE(deleted_at, #{deletedAt}), deleted_at = COALESCE(deleted_at, #{deletedAt})
    </insert>
</mapper>
